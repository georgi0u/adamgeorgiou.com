"use strict";
exports.__esModule = true;
function createColorMap(set) {
    var map = new Map();
    var index = 0;
    set.forEach(function (item) {
        map[item] = (360 / set.size) * index;
        index++;
        1;
    });
    return map;
}
exports.createColorMap = createColorMap;
